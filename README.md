# E-commerce Core

Реализация основных сущностей интернет-магазина.

## Функционал

- Класс `Product` для представления товаров
- Класс `Category` для представления категорий товаров
- Автоматический подсчет количества категорий и товаров
- Полная тестовая покрытие функционала

## Установка

1. Установите Poetry: https://python-poetry.org/docs/#installation
2. Установите зависимости: `poetry install`
3. Активируйте виртуальное окружение: `poetry shell`

## Запуск тестов

# E-commerce Core

## Тестовое покрытие

Отчет о покрытии тестами доступен в папке [htmlcov/](htmlcov/). 
Откройте файл `htmlcov/index.html` в браузере для просмотра детальной статистики.

**Текущее покрытие:** более 90%

## Просмотр отчета о покрытии

1. Клонируйте репозиторий
2. Откройте файл `htmlcov/index.html` в браузере
3. Или сгенерируйте отчет заново: `pytest --cov=src --cov-report=html`
4. # E-commerce Core - Домашнее задание 14.2

## Новая функциональность

### Режимы доступа к атрибутам
- Атрибут списка товаров класса `Category` стал приватным
- Реализован метод `add_product()` для добавления товаров
- Автоматическое обновление счетчика продуктов при добавлении

### Геттеры и сеттеры
- Геттер для приватного атрибута `products` возвращает форматированную строку
- Приватный атрибут цены в классе `Product` с геттером и сеттером
- Валидация цены на положительное значение

### Класс-методы
- Класс-метод `new_product()` для создания объектов из словаря
# E-commerce Core - Домашнее задание 15.1

## Новая функциональность

### Магические методы
- **`__str__` для Product**: Возвращает строку в формате "Название, цена руб. Остаток: X шт."
- **`__str__` для Category**: Возвращает строку с общим количеством товаров в категории
- **`__add__` для Product**: Позволяет складывать продукты, возвращая суммарную стоимость всех товаров на складе

### Оптимизация
- Геттер `products` класса Category теперь использует `__str__` каждого продукта
- Добавлен метод `get_total_quantity()` для удобного получения общего количества товаров
# E-commerce Core - Домашнее задание 16.1

## Новая функциональность

### Классы-наследники
- **Smartphone**: Наследник Product с дополнительными атрибутами:
  - `efficiency` (производительность)
  - `model` (модель)
  - `memory` (объем памяти)
  - `color` (цвет)
- **LawnGrass**: Наследник Product с дополнительными атрибутами:
  - `country` (страна-производитель)
  - `germination_period` (срок прорастания)
  - `color` (цвет)

### Ограничения сложения
- Можно складывать только товары одинакового типа
- При попытке сложить разные типы выбрасывается `TypeError`

### Ограничения добавления в категорию
- В категорию можно добавлять только объекты класса `Product` и его наследников
- Используется `isinstance()` для проверки

## Запуск тестов

```bash
pytest --cov=src --cov-report=html